version: '3.8'

services:
  goal-prediction-bot:
    build: .
    container_name: goal-prediction-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_APP_ID=${TELEGRAM_APP_ID}
    volumes:
      - ./logs:/app/logs